name: simple-wordpress
version: ${{ .Parameters.version }}
application:
  name: grafana
  partOf: ""
  version: "9000.1"
triggers:
  - deployment:
      name: deploy-trigger
  - web:
      name: web-trigger
  - manual:
      name: user-trigger
      users:
        - "Adam Page"
strategy:
  - template: test/yaml/template.yaml
    branch: feature/selector-naming
    repo: boatswain
    arguments:
      name: install-another-grafana
