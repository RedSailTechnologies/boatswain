apiVersion: v1
kind: Service
metadata:
  name: {{ include "boatswain.fullname" . }}
  labels:
    {{- include "boatswain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "boatswain.selectorLabels" . | nindent 4 }}
